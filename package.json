{
  "name": "@manzano/fetch-http-client",
  "version": "1.0.0",
  "main": "index.js",
  "config": {
    "distFolder": "dist"
  },
  "scripts": {
    "build": "rm -rf $npm_package_config_distFolder && tsc && cp package.json $npm_package_config_distFolder",
    "deploy": "npm publish $npm_package_config_distFolder --access public",
    "prettify": "prettier src --write"
  },
  "peerDependencies": {
    "@manzano/http-client": ">=1.0.4"
  },
  "devDependencies": {
    "@manzano/http-client": "1.0.4",
    "prettier": "2.0.5",
    "typescript": "3.9.7"
  },
  "dependencies": {
    "fetch-intercept": "^2.3.1",
    "js-base64": "^3.5.2"
  }
}
